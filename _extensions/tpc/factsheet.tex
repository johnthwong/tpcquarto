% TPC Factsheet LaTeX Template
\documentclass[$if(fontsize)$$fontsize$$else$10pt$endif$]{article}

\usepackage[top=2.0cm, bottom=0.5in, left=0.5in, right=0.5in, headheight=1cm, headsep=0cm]{geometry}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{multicol}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{tcolorbox}
\usepackage{fontspec}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{parskip}
\usepackage{float}
\usepackage{calc}
\usepackage{tikz}
% Line spacing
\linespread{1.25}

% Pandoc bounded image macro — constrain images to text width
\makeatletter
\newcommand{\pandocbounded}[1]{%
  \begingroup
  \setbox0=\hbox{#1}%
  \ifdim\wd0>\linewidth
    \resizebox{\linewidth}{!}{#1}%
  \else
    #1%
  \fi
  \endgroup
}
\makeatother

% TPC colors
\definecolor{deepblue}{RGB}{23,74,124}
\definecolor{teal}{RGB}{0,139,176}
\definecolor{poppy}{RGB}{240,87,62}
\definecolor{lightgray}{RGB}{188,190,192}
\definecolor{darkgray}{RGB}{92,88,89}
\definecolor{bglight}{RGB}{245,247,249}

% Font setup
\setmainfont{AvenirTPC}[
  Path = _extensions/tpc/fonts/,
  Extension = .ttf,
  UprightFont = *-Regular,
  BoldFont = *-Bold,
  ItalicFont = *-Italic,
  BoldItalicFont = *-BoldItalic
]

% Content margins (used inside the body)
\newlength{\contentmargin}
\setlength{\contentmargin}{0.75in}

% Section formatting — teal accent bar before section titles
\titleformat{\section}
  {\normalfont\large\bfseries\color{deepblue}}
  {}{0em}{}[\vspace{-0.3em}{\color{teal}\rule{2cm}{2pt}}]
\titlespacing*{\section}{0pt}{1.2em}{0.4em}

\titleformat{\subsection}
  {\normalfont\normalsize\bfseries\color{deepblue}\MakeUppercase}
  {}{0em}{}
\titlespacing*{\subsection}{0pt}{1em}{0.3em}

\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries\color{white}}
  {}{0em}{}

% Colored box environments
\newtcolorbox{box-deepblue}{
  colback=deepblue, colframe=deepblue,
  colupper=white, fontupper=\normalsize,
  boxrule=0pt, arc=3pt, left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=8pt, after skip=8pt
}
\newtcolorbox{box-teal}{
  colback=teal, colframe=teal,
  colupper=white, fontupper=\normalsize,
  boxrule=0pt, arc=3pt, left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=8pt, after skip=8pt
}
\newtcolorbox{box-light}{
  colback=bglight, colframe=bglight,
  boxrule=0pt, arc=3pt, left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=8pt, after skip=8pt
}

% Sidebar environment
\newtcolorbox{sidebarbox}{
  colback=white, colframe=teal,
  boxrule=0pt, leftrule=3pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=8pt, after skip=8pt
}

% Note box
\newtcolorbox{notebox}{
  colback=bglight, colframe=poppy,
  boxrule=0pt, leftrule=4pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  before skip=8pt, after skip=8pt
}

% Header/footer — continuation pages get a slim colored bar with logo
\fancypagestyle{continuation}{%
  \fancyhf{}%
  \renewcommand{\headrulewidth}{0pt}%
  \fancyhead[L]{%
    \begin{tikzpicture}[remember picture, overlay]
      \fill[deepblue] (current page.north west) rectangle
        ([yshift=-0.9cm]current page.north east);
      \fill[teal] ([yshift=-0.9cm]current page.north west) rectangle
        ([yshift=-0.95cm]current page.north east);
      \node[anchor=east, inner sep=0pt] at ([xshift=-0.5in, yshift=-0.45cm]current page.north east)
        {\includegraphics[height=0.6cm]{_extensions/tpc/logo.png}};
      \node[anchor=west, inner sep=0pt] at ([xshift=0.5in, yshift=-0.45cm]current page.north west)
        {{\small\bfseries\color{white} TAX POLICY CENTER \color{lightgray}| \color{white}URBAN INSTITUTE \& BROOKINGS INSTITUTION}};
    \end{tikzpicture}%
  }%
  \fancyfoot[R]{\small\color{lightgray}\thepage}%
}
\fancypagestyle{firstpage}{%
  \fancyhf{}%
  \renewcommand{\headrulewidth}{0pt}%
  \fancyfoot[R]{\small\color{lightgray}\thepage}%
}
\pagestyle{continuation}
\AtBeginDocument{\thispagestyle{firstpage}}

% Hyperlink styling
\hypersetup{
  colorlinks=true,
  linkcolor=deepblue,
  urlcolor=teal,
  citecolor=deepblue
}

% Tight list support (Pandoc)
\providecommand{\tightlist}{%
  \setlength{\itemsep}{2pt}\setlength{\parskip}{0pt}}

% Custom bullet style
\setlist[itemize]{leftmargin=1.2em, label={\color{teal}\textbullet}}

% CSL refs support (Pandoc)
$if(csl-refs)$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newenvironment{CSLReferences}[2]{}{}
$endif$

\begin{document}

% ── Full-width header banner (drawn over the top margin) ──
\begin{tikzpicture}[remember picture, overlay]
  \fill[deepblue] (current page.north west) rectangle
    ([yshift=-2.2cm]current page.north east);
  \fill[teal] ([yshift=-2.2cm]current page.north west) rectangle
    ([yshift=-2.3cm]current page.north east);
  % Logo — top right, square version
  \node[anchor=east, inner sep=0pt] at ([xshift=-0.5in, yshift=-1.1cm]current page.north east)
    {\includegraphics[height=1.5cm]{_extensions/tpc/logo.png}};
  % Title and subtitle — left side
  \node[anchor=west, inner sep=0pt, text width=0.6\paperwidth] at ([xshift=0.5in, yshift=-1.1cm]current page.north west)
    {{\fontsize{14}{17}\bfseries\color{white} $title$}%
    $if(subtitle)$\par\vspace{0.15em}{\fontsize{9}{12}\selectfont\color{lightgray} $subtitle$}$endif$%
    $if(date)$\par\vspace{0.15em}{\fontsize{9}{12}\selectfont\color{lightgray} $date$}$endif$};
\end{tikzpicture}%
\noindent

% ── Body content ──
$body$

\end{document}
